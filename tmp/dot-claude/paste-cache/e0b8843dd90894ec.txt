1. actually in the process of *using the telephone game as a act of actively forgetting implementation details with the goal to keep the overall 20.000m overview* by design - so interesting that the opposite is taken up as a example here :)
2. swarm is really effective when the goal is "solutions to get forward" and not so much when setting up the end results 

and writing to disk - using markdown files as your ram and not only memory, but active workspace, is *golden*

---

from 'skogai-prompting':

```
---
name: skogai-prompting
description: This skill should be used when building AI agents using prompt-native architecture where features are defined in prompts, not code. Use it when creating autonomous agents, designing MCP servers, implementing self-modifying systems, or adopting the "trust the agent's intelligence" philosophy.
---

<essential_principles>

## The Prompt-Native Philosophy

Agent native engineering inverts traditional software architecture. Instead of writing code that the agent executes, you define outcomes in prompts and let the agent figure out HOW to achieve them.

### The Foundational Principle

**Whatever the user can do, the agent can do. Many things the developer can do, the agent can do.**

Don't artificially limit the agent. If a user could read files, write code, browse the web, deploy an app—the agent should be able to do those things too. The agent figures out HOW to achieve an outcome; it doesn't just call your pre-written functions.

```

---

and i agree, our goal is to remove tool calls all together and essentially let the agent use plain text-intents just as i/the user does in practice.

and from the descriptions i'd argue that *when to use it* is 70% of the workload done.. 

and personally; no error messages should ever exist to begin with together with one tool per step - and one workflow should only ever exist in a context to begin with

